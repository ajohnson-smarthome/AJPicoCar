# Ramp (плавный разгон) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Плавный разгон (slew-rate): 50 Гц ramp-таск — единственный писатель в PCA9685, рост duty ограничен, падение мгновенно; `ramp_ms` в NVS + REST `/ramp` + слайдер в iOS-настройках.

**Architecture:** Чистая `ramp_step()` (static inline в `ramp.h`, хост-тест — паттерн `watchdog_stale`). `ramp.c` владеет `s_target/s_current` со своим мьютексом и таском; `car.c` вместо записи в I2C кладёт цели через `ramp_set_target()`. `ramp_api.c` — GET/POST `/ramp` + NVS. iOS — `RampClient` + слайдер в `SettingsView`.

**Tech Stack:** ESP-IDF 5.4 (FreeRTOS task, nvs), cc хост-тест, Swift 6 / SwiftUI. Ветка `ramp`. SDK `iphonesimulator26.2`, `iPhone 17`, мок.

---

## File Structure

| Файл | Изменение |
|---|---|
| `main/ramp.h` *(new)* | API + чистая `ramp_step()` (zero IDF deps) |
| `main/ramp.c` *(new)* | таск 50 Гц, `s_target/s_current`, NVS load/save `ramp_ms` |
| `main/ramp_api.{c,h}` *(new)* | `GET/POST /ramp` |
| `main/car.c` | `motors_apply` → `ramp_set_target` |
| `main/main.c` | `ramp_init()` после NVS, `ramp_api_start()` после httpd |
| `main/CMakeLists.txt` | + `ramp.c`, `ramp_api.c` |
| `test/test_ramp.c` *(new)* + `test/Makefile` | хост-тест `ramp_step` |
| `tools/mock_car/mock_car.py` | `GET/POST /ramp` |
| iOS: `RampClient.swift` *(new)*, `SettingsView.swift`, `L.swift`, `ru.lproj` | слайдер «Разгон» |

---

## Task 1: Чистая `ramp_step` (TDD)

**Files:** Create `main/ramp.h`, `test/test_ramp.c`; Modify `test/Makefile`.

- [ ] **Step 1: Создать `main/ramp.h`**
```c
#ifndef RAMP_H
#define RAMP_H
#include <stdint.h>

// Pure slew-rate step: rise is limited to max_up per call, fall is instant (safe stop).
// Zero ESP-IDF deps — host-tested.
static inline uint16_t ramp_step(uint16_t current, uint16_t target, uint16_t max_up) {
    if (target <= current) return target;                 // fall (or equal): instant
    uint32_t next = (uint32_t)current + max_up;           // rise: bounded
    return next > target ? target : (uint16_t)next;
}

#ifndef RAMP_HOST_TEST
#include "esp_err.h"
// Start the 50 Hz ramp task (sole PCA9685 writer). Loads ramp_ms from NVS (default 300).
esp_err_t ramp_init(void);
// Set the 8-channel duty target (copied under the ramp lock).
void ramp_set_target(const uint16_t duty[8]);
// Acceleration time 0→full in ms (0 = ramp off / instant). Clamped to 0..2000. Persisted by caller.
void ramp_set_ms(uint16_t ms);
uint16_t ramp_get_ms(void);
#endif
#endif // RAMP_H
```

- [ ] **Step 2: Создать `test/test_ramp.c`**
```c
#define RAMP_HOST_TEST
#include "../main/ramp.h"
#include <assert.h>
#include <stdio.h>

int main(void) {
    // rise is bounded
    assert(ramp_step(0, 4095, 100) == 100);
    assert(ramp_step(100, 4095, 100) == 200);
    // rise lands exactly on target (no overshoot)
    assert(ramp_step(4000, 4095, 100) == 4095);
    assert(ramp_step(4095, 4095, 100) == 4095);
    // fall is instant
    assert(ramp_step(4095, 0, 100) == 0);
    assert(ramp_step(2000, 1999, 1) == 1999);
    // ramp off (max_up = 4095) reaches full in one step
    assert(ramp_step(0, 4095, 4095) == 4095);
    // no uint16 overflow near the top
    assert(ramp_step(4090, 4095, 4095) == 4095);
    printf("test_ramp: all passed\n");
    return 0;
}
```

- [ ] **Step 3: Добавить в `test/Makefile`** — `test_ramp` в список целей по образцу существующих (`BINS`/правила: цель `test_ramp: test_ramp.c` без extra-объектов, как `test_watchdog`).

- [ ] **Step 4: Запустить** `cd test && make run` — Expected: `test_ramp: all passed` (и остальные зелёные).

- [ ] **Step 5: Commit**
```bash
cd /Users/adamjohnson/VSCode/esp32-p4-car
git add main/ramp.h test/test_ramp.c test/Makefile
git commit -m "feat: pure ramp_step (bounded rise, instant fall) + host test"
```

---

## Task 2: Ramp-таск + перевод `car.c` на цели

**Files:** Create `main/ramp.c`; Modify `main/car.c`, `main/main.c`, `main/CMakeLists.txt`.

- [ ] **Step 1: Создать `main/ramp.c`**
```c
#include "ramp.h"
#include <string.h>
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "freertos/semphr.h"
#include "esp_log.h"
#include "nvs.h"
#include "pca9685.h"

static const char *TAG = "ramp";
#define NS  "car"
#define KEY "ramp_ms"
#define TICK_MS 20            // 50 Hz
#define RAMP_MS_DEFAULT 300
#define RAMP_MS_MAX 2000

static SemaphoreHandle_t s_lock;          // protects s_target + s_ramp_ms
static uint16_t s_target[8];
static uint16_t s_current[8];
static uint16_t s_ramp_ms = RAMP_MS_DEFAULT;

static uint16_t max_up_per_tick(uint16_t ramp_ms) {
    if (ramp_ms < TICK_MS) return 4095;                  // 0 (off) or < one tick: instant
    uint16_t step = (uint16_t)(4095u * TICK_MS / ramp_ms);
    return step ? step : 1;
}

static void ramp_task(void *arg) {
    (void)arg;
    TickType_t last = xTaskGetTickCount();
    for (;;) {
        vTaskDelayUntil(&last, pdMS_TO_TICKS(TICK_MS));
        uint16_t tgt[8], up;
        if (xSemaphoreTake(s_lock, pdMS_TO_TICKS(TICK_MS)) != pdTRUE) continue;
        memcpy(tgt, s_target, sizeof(tgt));
        up = max_up_per_tick(s_ramp_ms);
        xSemaphoreGive(s_lock);

        bool dirty = false;
        for (int ch = 0; ch < 8; ch++) {
            uint16_t next = ramp_step(s_current[ch], tgt[ch], up);
            if (next != s_current[ch]) { s_current[ch] = next; dirty = true; }
        }
        if (!dirty) continue;
        for (uint8_t ch = 0; ch < 8; ch++) {            // sole I2C writer after init
            esp_err_t e = pca9685_set_pwm(ch, s_current[ch]);
            if (e != ESP_OK) ESP_LOGE(TAG, "ch%d write failed: %s", ch, esp_err_to_name(e));
        }
    }
}

void ramp_set_target(const uint16_t duty[8]) {
    if (s_lock && xSemaphoreTake(s_lock, pdMS_TO_TICKS(200)) != pdTRUE) {
        ESP_LOGW(TAG, "set_target: lock busy, frame dropped");
        return;
    }
    memcpy(s_target, duty, sizeof(s_target));
    if (s_lock) xSemaphoreGive(s_lock);
}

void ramp_set_ms(uint16_t ms) {
    if (ms > RAMP_MS_MAX) ms = RAMP_MS_MAX;
    if (s_lock && xSemaphoreTake(s_lock, pdMS_TO_TICKS(200)) != pdTRUE) return;
    s_ramp_ms = ms;
    if (s_lock) xSemaphoreGive(s_lock);
    ESP_LOGI(TAG, "ramp_ms = %u", ms);
}

uint16_t ramp_get_ms(void) {
    return s_ramp_ms;   // u16 read is atomic on riscv32
}

esp_err_t ramp_init(void) {
    s_lock = xSemaphoreCreateMutex();
    if (!s_lock) return ESP_ERR_NO_MEM;

    nvs_handle_t h;
    if (nvs_open(NS, NVS_READONLY, &h) == ESP_OK) {
        uint16_t v;
        if (nvs_get_u16(h, KEY, &v) == ESP_OK && v <= RAMP_MS_MAX) s_ramp_ms = v;
        nvs_close(h);
    }
    ESP_LOGI(TAG, "ramp_ms = %u (boot)", s_ramp_ms);

    return xTaskCreate(ramp_task, "ramp", 3072, NULL, 5, NULL) == pdPASS ? ESP_OK : ESP_FAIL;
}
```

- [ ] **Step 2: `main/car.c` — цели вместо прямой записи.** Заменить `motors_apply` и его вызовы:
  - удалить функцию `motors_apply` и `#include "pca9685.h"`; добавить `#include "ramp.h"`;
  - в `car_drive`: `motors_apply(&out);` → `ramp_set_target(out.duty);`
  - в `car_spin_pair`: `motors_apply(&out);` → `ramp_set_target(out.duty);`
  - комментарий у `g_lock` поправить: он защищает `g_cfg`/план, I2C теперь у ramp-таска:
```c
// Serializes g_cfg access and target planning for concurrent callers (console + WS + httpd).
// The PCA9685 itself is written only by the ramp task (ramp.c).
```

- [ ] **Step 3: `main/main.c`** — добавить `#include "ramp.h"`; после `ESP_ERROR_CHECK(nvs);` и ПЕРЕД `car_init();` вставить:
```c
    ESP_ERROR_CHECK(ramp_init());   // start the sole PCA9685 writer before any car_* call
```

- [ ] **Step 4: `main/CMakeLists.txt`** — добавить `"ramp.c"` в SRCS.

- [ ] **Step 5: Build**
```bash
mkdir -p /tmp/py313bin && ln -sf /opt/homebrew/bin/python3.13 /tmp/py313bin/python3
cd /Users/adamjohnson/VSCode/esp32-p4-car && export PATH=/tmp/py313bin:$PATH && source ~/esp/esp-idf/export.sh >/dev/null 2>&1
idf.py build 2>&1 | grep -iE "Project build complete|error:" | grep -viE "rv32|march" | tail -3
```
Expected: `Project build complete`.

- [ ] **Step 6: Commit**
```bash
cd /Users/adamjohnson/VSCode/esp32-p4-car
git add main/ramp.c main/car.c main/main.c main/CMakeLists.txt
git commit -m "feat: 50Hz ramp task — sole PCA9685 writer; car.c posts duty targets"
```

---

## Task 3: REST `/ramp` + сохранение в NVS

**Files:** Create `main/ramp_api.h`, `main/ramp_api.c`; Modify `main/main.c`, `main/CMakeLists.txt`.

- [ ] **Step 1: `main/ramp_api.h`**
```c
#ifndef RAMP_API_H
#define RAMP_API_H
#include "esp_err.h"
// Register GET/POST /ramp on the shared httpd (acceleration time, ms; persisted to NVS).
esp_err_t ramp_api_start(void);
#endif // RAMP_API_H
```

- [ ] **Step 2: `main/ramp_api.c`**
```c
#include "ramp_api.h"
#include <stdio.h>
#include <stdlib.h>
#include "esp_http_server.h"
#include "esp_log.h"
#include "esp_check.h"
#include "nvs.h"
#include "http_server.h"
#include "ramp.h"

static const char *TAG = "ramp_api";

static esp_err_t ramp_get(httpd_req_t *req) {
    char buf[32];
    int n = snprintf(buf, sizeof(buf), "{\"ramp_ms\":%u}", ramp_get_ms());
    httpd_resp_set_type(req, "application/json");
    return httpd_resp_send(req, buf, n);
}

static esp_err_t ramp_post(httpd_req_t *req) {
    char body[16] = {0};
    int len = httpd_req_recv(req, body, sizeof(body) - 1);
    if (len <= 0) { httpd_resp_send_err(req, HTTPD_400_BAD_REQUEST, "empty"); return ESP_FAIL; }
    char *end;
    long v = strtol(body, &end, 10);
    if (end == body || v < 0 || v > 2000) {
        httpd_resp_send_err(req, HTTPD_400_BAD_REQUEST, "ramp_ms must be 0..2000");
        return ESP_FAIL;
    }
    ramp_set_ms((uint16_t)v);
    nvs_handle_t h;
    if (nvs_open("car", NVS_READWRITE, &h) == ESP_OK) {
        if (nvs_set_u16(h, "ramp_ms", (uint16_t)v) == ESP_OK) nvs_commit(h);
        nvs_close(h);
    }
    return httpd_resp_sendstr(req, "ok");
}

esp_err_t ramp_api_start(void) {
    httpd_handle_t server = http_server_get_handle();
    if (server == NULL) { ESP_LOGE(TAG, "http server not started"); return ESP_FAIL; }
    httpd_uri_t g = { .uri = "/ramp", .method = HTTP_GET, .handler = ramp_get };
    httpd_uri_t p = { .uri = "/ramp", .method = HTTP_POST, .handler = ramp_post };
    ESP_RETURN_ON_ERROR(httpd_register_uri_handler(server, &g), TAG, "reg GET /ramp");
    ESP_RETURN_ON_ERROR(httpd_register_uri_handler(server, &p), TAG, "reg POST /ramp");
    return ESP_OK;
}
```

- [ ] **Step 3: `main/main.c`** — `#include "ramp_api.h"`; после `ESP_ERROR_CHECK(ota_api_start());` добавить `ESP_ERROR_CHECK(ramp_api_start());`

- [ ] **Step 4: `main/CMakeLists.txt`** — добавить `"ramp_api.c"` в SRCS.

- [ ] **Step 5: Build** (как Task 2 Step 5). Expected: `Project build complete`.

- [ ] **Step 6: Commit**
```bash
cd /Users/adamjohnson/VSCode/esp32-p4-car
git add main/ramp_api.h main/ramp_api.c main/main.c main/CMakeLists.txt
git commit -m "feat: GET/POST /ramp — acceleration time, persisted to NVS"
```

---

## Task 4: Мок `/ramp`

**Files:** Modify `tools/mock_car/mock_car.py`.

- [ ] **Step 1: Добавить** (рядом с `ota`; `STATE` уже есть — добавить ключ):
```python
STATE.setdefault("ramp_ms", 300)

async def ramp_get(request):
    return web.json_response({"ramp_ms": STATE["ramp_ms"]})

async def ramp_post(request):
    body = (await request.text()).strip()
    try:
        v = int(body)
        if not 0 <= v <= 2000:
            raise ValueError
    except ValueError:
        return web.Response(status=400, text="ramp_ms must be 0..2000")
    STATE["ramp_ms"] = v
    print(f"ramp: set {v} ms")
    return web.Response(text="ok")
```
И в `app.add_routes([...])`: `web.get("/ramp", ramp_get), web.post("/ramp", ramp_post),`

- [ ] **Step 2: Smoke**
```bash
cd /Users/adamjohnson/VSCode/esp32-p4-car/tools/mock_car
pkill -f mock_car.py 2>/dev/null; sleep 1; nohup .venv/bin/python -u mock_car.py >/tmp/mock_car.log 2>&1 & disown; sleep 2
curl -s http://127.0.0.1:8080/ramp; echo
curl -s -X POST --data "550" http://127.0.0.1:8080/ramp; echo
curl -s http://127.0.0.1:8080/ramp; echo
```
Expected: `{"ramp_ms": 300}` → `ok` → `{"ramp_ms": 550}`.

- [ ] **Step 3: Commit**
```bash
cd /Users/adamjohnson/VSCode/esp32-p4-car
git add tools/mock_car/mock_car.py && git commit -m "feat(tools): mock /ramp"
```

---

## Task 5: iOS — слайдер «Разгон» в настройках

**Files:** Create `ios/ESP32Car/RampClient.swift`; Modify `ios/ESP32Car/SettingsView.swift`, `ios/ESP32Car/L.swift`, `ios/ESP32Car/Resources/ru.lproj/Localizable.strings`.

- [ ] **Step 1: Строки** (`Localizable.strings`):
```
"ramp.title"  = "Разгон";
"ramp.label"  = "Плавный старт: %d мс";
"ramp.off"    = "Плавный старт: выкл";
```

- [ ] **Step 2: Аксессоры** (`L.swift`):
```swift
    static var rampTitle: String { s("ramp.title") }
    static var rampOff: String { s("ramp.off") }
    static func rampLabel(_ ms: Int) -> String { s("ramp.label", ms) }
```

- [ ] **Step 3: `ios/ESP32Car/RampClient.swift`**
```swift
import Foundation

/// Reads/writes the car's acceleration-ramp time (ms) via GET/POST /ramp.
struct RampClient {
    func get() async -> Int? {
        guard let url = URL(string: CarHost.httpBase + "/ramp") else { return nil }
        guard let (data, _) = try? await URLSession.shared.data(from: url),
              let j = try? JSONSerialization.jsonObject(with: data) as? [String: Any],
              let v = j["ramp_ms"] as? Int else { return nil }
        return v
    }
    @discardableResult
    func set(_ ms: Int) async -> Bool {
        guard let url = URL(string: CarHost.httpBase + "/ramp") else { return false }
        var req = URLRequest(url: url)
        req.httpMethod = "POST"
        req.httpBody = String(ms).data(using: .utf8)
        guard let (_, resp) = try? await URLSession.shared.data(for: req) else { return false }
        return (resp as? HTTPURLResponse)?.statusCode == 200
    }
}
```

- [ ] **Step 4: `SettingsView.swift` — секция «Разгон»** внутри существующего `List` (после строки «Прошивка»):
```swift
                    Section {
                        VStack(alignment: .leading, spacing: 6) {
                            Text(rampMs > 0 ? L.rampLabel(rampMs) : L.rampOff)
                                .font(.system(size: 14)).foregroundStyle(palette.text)
                            Slider(value: Binding(
                                get: { Double(rampMs) },
                                set: { rampMs = Int($0 / 50) * 50 }
                            ), in: 0...1000) { editing in
                                if !editing { Task { await RampClient().set(rampMs) } }
                            }
                            .tint(palette.accent)
                        }
                        .listRowBackground(palette.panel)
                    } header: {
                        Text(L.rampTitle).foregroundStyle(palette.muted)
                    }
```
Плюс стейт и загрузка в `SettingsView`:
```swift
    @State private var rampMs = 300
```
и на корневом контейнере (`.task` к List/NavigationStack):
```swift
        .task { if let v = await RampClient().get() { rampMs = v } }
```
(Точные точки вставки — по текущей структуре файла; сохранить стиль/фон панелей.)

- [ ] **Step 5: Build + grep**
```bash
cd /Users/adamjohnson/VSCode/esp32-p4-car/ios && xcodegen generate && xcodebuild build -target ESP32Car -sdk iphonesimulator26.2 ARCHS=arm64 2>&1 | grep -iE "error:|BUILD SUCCEEDED|BUILD FAILED" | head -6
```
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 6: Commit**
```bash
cd /Users/adamjohnson/VSCode/esp32-p4-car
git add ios/ESP32Car/RampClient.swift ios/ESP32Car/SettingsView.swift ios/ESP32Car/L.swift ios/ESP32Car/Resources
git commit -m "feat(ios): ramp slider in settings (GET/POST /ramp)"
```

---

## Task 6: Проверка

- [ ] **Step 1: Host-тесты + прошивка**: `cd test && make run` (все зелёные, вкл. `test_ramp`); `idf.py build` ок.
- [ ] **Step 2: Симулятор**: мок запущен → ⚙ Настройки → секция «Разгон»: слайдер показывает 300 мс (с мока), подвинуть → отпустить → `grep "ramp: set" /tmp/mock_car.log` показывает новое значение; перезайти в настройки → значение сохранилось. Скриншот.
- [ ] **Step 3: Бенч (пользователь, позже)**: прошить → старт мягкий, без браунаута; стоп мгновенный; подобрать `ramp_ms` слайдером; одиночный консольный `mix 1 0` доезжает до полной скорости.

---

## Self-Review заметки

- **Покрытие спеки:** чистая `ramp_step` + хост-тест (Task 1); 50 Гц таск — единственный I2C-писатель, мгновенное падение, `car.c` на целях (Task 2); NVS + `GET/POST /ramp` с валидацией 0..2000 (Task 3); мок (Task 4); iOS-слайдер 0–1000/50 + строки (Task 5); проверка (Task 6).
- **Тип-консистентность:** `ramp_step(u16,u16,u16)`, `ramp_init/ramp_set_target/ramp_set_ms/ramp_get_ms` (h↔c↔вызовы); `RampClient.get/set`; `L.rampTitle/rampOff/rampLabel`; NVS ns `"car"`, ключ `"ramp_ms"` (load в ramp.c, save в ramp_api.c — одинаковые литералы).
- **Конкуренция:** `s_lock` (ramp) отдельно от `g_lock` (car): car-вызовы берут g_lock→затем ramp_set_target берёт s_lock; ramp-таск берёт только s_lock — иерархия односторонняя, дедлока нет. `vTaskDelayUntil` — стабильные 50 Гц. `dirty`-флаг: нет лишних I2C, когда current==target.
- **Безопасность:** `car_stop` → цель 0 → следующий тик (≤20 мс) применяет мгновенно (fall instant). Watchdog-путь не изменился.
- **Замечания:** `car_spin_pair` тоже под ramp (мягкий старт идентификации — ок, ~100 мс до 1600). `ramp_get_ms` без лока — u16 read атомарен. Точки вставки в SettingsView подогнать по факту структуры файла.
